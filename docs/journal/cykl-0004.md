# Cykl 4

Wejście: `docs/journal/cykl-0003.md`, sekcja `## Stan`. Raporty: #55, #56, #57, #58, #59, #60, #61.
Otwartej `awaria` brak.

## Zlecone

Mapa cyklu 4 — issue złączeniowe [#84](../../issues/84) (`loop:map`, sub-issues podpięte).

| issue | rola | co | blokady |
|---|---|---|---|
| [#77](../../issues/77) | implementer | odzyskaj z `origin/task/58` i `origin/task/59` dorobek, który nie dojechał na gałąź domyślną; scal dwa przeszukania tacki w jedno | — |
| [#78](../../issues/78) | implementer | odzyskaj `bridge/runs/d550db3/` z `origin/task/60` i rozstrzygnij Z-6 na tych danych; zaraportuj, nie naprawiaj | — |
| [#79](../../issues/79) | implementer | zmierz koszt decyzji `TrayPolicy` i dobierz domyślny `beam` | ← #77 |
| [#80](../../issues/80) | implementer | `benchmark.py` ma umieć wziąć strojone wagi (`heuristic:<plik>`, `tray:<plik>`) | ← #77 |
| [#81](../../issues/81) | implementer | pełny przebieg CEM → `weights.json` + `docs/strojenie-wag.md`; commit po każdej generacji | ← #77, #79 |
| [#82](../../issues/82) | bench | pełny benchmark strojonych wag kontra zachłanna — **polecenia w `## Weryfikacja`** | ← #80, #81 |
| [#84](../../issues/84) | orchestrator | domknięcie cyklu 4 i mapa cyklu 5 | ← wszystkie sześć |

Zadispatchowane od razu: #77, #78 (jedyne z `blocked_by = 0`). Resztę rusza epilog.

Poza pętlą, dla właściciela (bez `rola:*`): [#83](../../issues/83) — `rola:bench` melduje `done`,
choć nic nie policzyła.

## Liczby

- **Rekordu nadal nie ma.** `bench/record.json` nie istnieje. Ani jednych wag w repo.
- **Linia odniesienia bez zmian: 704,79 śr. / 34,99 postawień** (zachłanna, 300 stałych seedów,
  ε = 0, `move_cap` 2000; `bench/43d1e7cf…json`, #17). Stoi **czwarty cykl**.
- **Cykli od ostatniego rekordu: nie da się policzyć — rekordu nie było nigdy.** To jest
  właściwa liczba do zapisania i jest gorsza niż jakiekolwiek „N cykli temu".
- **Kandydat z #61: nie istnieje.** Job benchmarku nie policzył niczego (patrz `## Odkrycia`).
  `capped_pct` z #61 **nie ma**, więc sufitu ruchów nie ruszam — i tak ostatni znany pomiar
  to 0,0 (#17, potwierdzone niezależnie przez #51).
- **Jedyna nowa liczba cyklu 3, i jest miarodajna:** `HeuristicPolicy` z **ręcznymi** wagami,
  pełne 300 stałych seedów — **762,5 śr. / 36,84 postawień** kontra 704,79 / 34,99. Sparowana
  różnica **+8,19%**, poniżej progu `threshold_pct` = 10, więc etykieta benchmarku to „bez
  zmian". p10 spadło (160,0 → 139,9): heurystyka ma **gorszy dolny ogon** niż zachłanna mimo
  wyższej średniej (#57, `docs/cechy-planszy.md`).
- **Rozgałęzienie w obrębie znanej tacki:** średnia 39,66, mediana 32,0, maksimum 170
  (10 496 ruchów, 300 partii zachłannej; #57).
- **Most:** 100/100 ruchów zgodnych z symulacją, najdłuższa seria 100, **zero zatrzymań na
  nieznanym oknie** (#60). 33 pary „plansza → tacka", 0 nierozpoznanych kształtów.
- Dystans do celu: **~13 000×** licząc od 762,5.

## Odkrycia i obalone założenia

**1. Główne pytanie cyklu 3 nie ma odpowiedzi, bo pomiar się nie odbył — i to nie jest wina
linii pracy.** Trzy niezależne awarie mechaniki zjadły łańcuch #57 → #58 → #59 → #61:

- **#58 `crashed`.** Ostatni raport mówi „zrobione i scommitowane: (jeszcze nic)", ale to
  nieprawda: commit **`0877d99`** (`TrayPolicy` w `policies.py`, wpięcie `tray` do
  `benchmark.py`, `tests/test_tray_policy.py`, 222 linie) **istnieje na `origin/task/58`**.
  Sesja zdążyła scommitować i zginęła przed nadpisaniem raportu. **Nauka: przy `crashed`
  nie wierzę raportowi co do zawartości gałęzi — sprawdzam `git log origin/task/<n>`.**
  Gdybym uwierzył raportowi, zleciłbym `TrayPolicy` drugi raz od zera.
- **#59 `crashed`.** Commit **`c7eaf92`** (`tools/tune_weights.py`, CEM, 310 linii +
  148 linii testów) na `origin/task/59`. Właściwy przebieg CEM sesja puściła **w tle** i
  zginęła razem z nim. `weights.json` nigdy nie powstał.
- **Żadna z tych gałęzi nie dojechała na `main`.** `merge_main` scala kod tylko przy `done`;
  poprawka właściciela `d89a5dd` (#66) rozszerzyła to o dziennik i raport, ale **nie o kod**
  — i słusznie, bo kod niedokończonej sesji nie ma zielonych testów z definicji. Wniosek dla
  mnie: **odzyskanie dorobku gałęzi po `crashed` jest moim zadaniem, nie mechaniki.** Stąd #77.

**2. `rola:bench` zameldowała `done` i nie policzyła nic. To moja pomyłka w treści issue,
nie usterka pętli.** `.github/loop/loop.py:414` — `bench()` to `run_verification(body)`: job
**wykonuje polecenia z sekcji `## Weryfikacja`** i nie ma innego wejścia. W #61 `## Weryfikacja`
brzmiała `ls bench/*.json`. Plik linii bazowej z #17 leży w `bench/`, więc `ls` zwróciło 0,
a `loop.py:514` wpisało `("done", "Benchmark policzony.")`. Nic się nie policzyło.

> **Reguła dla każdego następnego cyklu: w issue `rola:bench` sekcja `## Weryfikacja` JEST
> przebiegiem.** Musi zawierać właściwe wywołanie `python3 benchmark.py --candidate … --issue N`.
> Nie wpisuje się tam `ls`, `test -f` ani niczego, co przechodzi bez policzenia.

Poprawka po stronie mechaniki (job bencha powinien paść, gdy nie powstał `bench/<sha>.json`)
leży w `.github/loop/loop.py`, którego role nie dotykają → zgłoszone jako **#83**.

**3. `benchmark.py` fizycznie nie umie zmierzyć strojonych wag — nawet gdyby `weights.json`
istniał.** `build_policy` (`benchmark.py:89-114`) przyjmuje `random`/`greedy`/`heuristic`
(+ `tray` z `0877d99`) z **wagami domyślnymi**, a każdą ścieżkę do pliku traktuje jako
`state_dict` torcha dla `Agent`. `weights.json` z CEM (`{"weights": [...]}`, 6 liczb) wpadłby
w gałąź torcha i wyszedł jako `ArmUnavailable`. Czyli **cały łańcuch cyklu 3 był z góry
niemierzalny**, niezależnie od crashy. Tego nie wyłapałem, pisząc mapę cyklu 3 — pisałem
`## Kontekst` #61 („kandydat: polityka z wagami z `weights.json`") bez sprawdzenia, czy
`benchmark.py` w ogóle umie taki kandydat zbudować. Stąd #80, postawione **przed** bench.

**4. `reward_shape_changed: yes` wystąpiło dokładnie raz, w #56, czyli tam, gdzie miało prawo.**
Sprawdziłem zmianę: `game.py:59` zwraca `gained` zamiast stałej `-5`, gałąź `wrong_placement`
(linia 52) bez zmian merytorycznych, dopisany komentarz z kontraktem. Nowy test
`TestTerminalReward.test_reward_sum_equals_final_score_for_greedy_policy` pilnuje, że suma
nagród epizodu równa się `game.score`. **To jest to, co chcę optymalizować** — nagroda epizodu
jest teraz tożsama z metryką benchmarku, więc nie ma już rozjazdu między tym, co agent maksymalizuje,
a tym, czym go mierzę. #57, #58, #59 zgłosiły `reward_shape_changed: no`. Żadnego cichego
przypadku nie było.

**5. Most żyje i to jest najlepsza wiadomość cyklu.** `rola:verifier` nie była zlecana od cyklu 1
i był to realny znak zapytania w `## Stanie` cyklu 3. #60 przeszło **100 ruchów bez jednej
rozbieżności z symulatorem** (`ok: true` × 100, najdłuższa seria = 100) i bez zatrzymania na
nieznanym oknie. **Licznik `strata_okna` zostaje na zerze**; ostatnie znane okna (reklama,
„Rating") nie wystąpiły. Zgodność 100/100 mówi też coś o samym symulatorze: mechanika
postawienia klocka i czyszczenia linii zgadza się z oryginałem na tej długości.

**6. `## Weryfikacja` #60 nie trafiła w ścieżkę, którą skill verifiera nakazuje.** Napisałem
`ls bridge/runs/*.json`, a skill wymaga `bridge/runs/<sha>/`. Sesja zachowała się wzorowo:
**nie spłaszczyła ścieżki i nie przerobiła polecenia**, tylko zaraportowała rozjazd i oddała
`partial`. Koszt tej mojej literówki to jedna sesja wypchnięta ze statusu `done`, a przez to
dorobek #60 niescalony na `main` (stąd #78). Nauka: `## Weryfikacja` piszę **pod ścieżkę
wymuszoną przez skill roli**, nie pod tę, którą sobie wyobrażam — dla verifiera
`ls bridge/runs/*/*.json`.

**7. Ocena raportów badawczych: w tym cyklu nie było ani jednego** — cykl 3 nie zlecał
`rola:researcher`. Raport #54 (`docs/research/kierunek-algorytmiczny.md`), na którym stoi
cała linia pracy, **nadal nie został sprawdzony pomiarem** i to jest jedyny powód, dla którego
nie umiem ocenić jego użyteczności. Jego kluczowa teza — „te same cechy dają 5 mln linii
z wagami ręcznymi i 51 mln ze strojonymi" — to twierdzenie `[Z]` i zamieniam je w pomiar (#81
+ #82), nie we wdrożenie. Pierwsza połowa tej tezy dostała w cyklu 3 pośrednie wsparcie:
ręczne wagi dały +8,19%, czyli dokładnie tyle, ile ręczne wagi mają dawać — mało.

**8. Uprawnienia sesji, dwa drobiazgi warte zapisania, żadnego nie naprawiam.** Verifier nie
może uruchomić `bash tools/emulator_probe.sh` (profil ma prefiksy `adb`, `python`, `python3`,
`git`, nie `bash`) — #60 odtworzyło kroki skryptów pojedynczymi komendami i to **wystarczyło**,
więc nie rozszerzam profilu na `Bash(bash:*)` tylko po to, żeby było wygodniej: to otworzyłoby
verifierowi całą powłokę. Wpisuję za to obejście w `## Budżet` przyszłych issues verifiera.
`Bash(python3:*)` dla implementera i verifiera właściciel dodał (`5578467`, #67) — obejście
„wołaj `python`" z cyklu 3 jest **nieaktualne** i nie przepisuję go do issues cyklu 4.

## Decyzja

**Linia pracy „funkcja oceny planszy + przeszukanie tacki + CEM" ZOSTAJE.** To jest odroczenie
werdyktu, nie jego wydanie, i zgodnie z kryterium z #62 mówię wprost, czego zabrakło:
**zabrakło samego pomiaru.** Nie mam prawa uznać linii za obaloną na podstawie +8,19% od
`HeuristicPolicy`, bo to wagi **ręczne**, a cała teza #54 mówi, że wartość siedzi w strojeniu,
którego nikt nie uruchomił. Obalanie linii pracy danymi, których nie ma, byłoby gorsze niż
jedna iteracja więcej.

**Ale odraczam z zapisanym progiem, żeby nie odraczać w nieskończoność.** Próg stawiam teraz,
przed pomiarem, na wyniku #82 (`tray` ze strojonymi wagami, 300 stałych seedów):

| wynik #82 | co robi cykl 5 |
|---|---|
| **≥ 2100 śr. (≥ 3× zachłanna)** | linia zostaje, iterujemy: głębsze przeszukanie, więcej cech, dłuższy CEM |
| **1060 – 2100 (1,5× – 3×)** | **jedna** iteracja poprawy przeszukania i ponowny pomiar; drugiego odroczenia nie ma |
| **< 1060 śr. (< 1,5×)** | linia dowiozła procenty, nie krotności → **zmiana kierunku** na podstawie `docs/research/kierunek-algorytmiczny.md`, sekcja (a) |

Uzasadnienie progu: cel to 14 000× linii bazowej, a #54 przypisuje samemu strojeniu
mnożnik rzędu 10× nad wagami ręcznymi. Jeżeli pierwszy pełny CEM nad przeszukaniem tacki
nie daje choćby 3×, to twierdzenie z #54 nie przenosi się na tę grę i dalsze dokładanie
cykli do tej samej linii jest kosztem, nie postępem.

**Kolejność cyklu 4 jest odwrotna niż w cyklu 3 i to jest właściwa poprawka.** Cykl 3 budował
politykę przez cztery zadania i mierzył na końcu — przez co trzy awarie skasowały wszystko.
Cykl 4 najpierw **odzyskuje** (#77, #78), potem **udrażnia pomiar** (#80), dopiero potem
produkuje kandydata (#79, #81) i mierzy (#82). Najdroższe i najbardziej kruche zadanie
(przebieg CEM) ma teraz w treści wymóg **commitowania po każdej generacji**, żeby kolejny
`crashed` nie kosztował całego przebiegu.

**Nagroda: nic nie zmieniam.** Decyzja #56 jest wykonana i zweryfikowana testem, obie sprawy
z `## Stanu` cyklu 3 (kara terminalna, kara za ruch nielegalny) zostają zamknięte.

**Z-6: nie rozstrzygam sam, bo nie mam danych pod ręką** — leżą na niescalonej gałęzi
`origin/task/60`. 33 pary to materiał wystarczający, żeby na pytanie odpowiedzieć
(albo uczciwie powiedzieć, że 33 to za mało), więc **zlecam analizę (#78) zamiast kolejnej
sesji mostu**. Ponowne uruchomienie emulatora po to, żeby zebrać dane, które już są, byłoby
marnotrawstwem najdroższej roli w pętli. #78 ma w `## Cel` „zaraportuj, nie naprawiaj" —
zmiana `generator.py` to osobna decyzja, moja, po przeczytaniu wyniku.

**Sufit ruchów: nie podnoszę.** Warunek to `capped_pct` > 5%, a ostatnie dwa niezależne
pomiary dają 0,0 (#17, #51). Brak danych z #61 nie jest przesłanką do ruszania sufitu.

**Weryfikacji na oryginale nie zlecam** — warunek to średnia ≥ 10 mln, jesteśmy przy 762,5.
#78 to analiza zebranych danych, nie łańcuch weryfikacyjny; te dwie rzeczy są niezależne
i wolno je prowadzić osobno.

**Rokowanie.** Linia rokuje, ale **kredyt jest już policzony i kończy się w cyklu 5**: cztery
cykle bez rekordu i bez ani jednego kandydata zmierzonego na 300 seedach to nie jest stan,
w którym wolno jeszcze raz powiedzieć „zobaczymy w następnym". Ryzyko, którego nie chowam:
przeżycie rośnie wolniej niż wynik (34,99 → 36,84 przy +8,19% punktów), a 10 mln pkt wymaga
partii o dwa rzędy dłuższych, nie tylko lepiej punktowanych — jeśli strojenie podniesie
punkty, a nie długość partii, to linia trafia w sufit własnej konstrukcji i cykl 5 to zobaczy
w liczbie postawień z #82, nawet gdy średnia przekroczy próg.

## Stan

**Cel.** Średnia ≥ 10 mln na 300 stałych seedach przy ε = 0 (#8) **i** jedna realna partia
≥ 1 mln w apce (#9), liczona naszym wzorem z trajektorii mostu, nie licznikiem apki (#20, #31).
Oba potwierdzone raportami. Wtedy: plik `GOAL_REACHED`, przypięty issue, `RAPORT.md` końcowy,
koniec startowania sesji.

**Gdzie jesteśmy.** Rekordu nie ma i **nie było nigdy** — `bench/record.json` nie istnieje.
Linia odniesienia: zachłanna **704,79 śr. / 34,99 postawień** (300 stałych seedów, ε = 0,
`move_cap` 2000; `bench/43d1e7cf…json`, #17) — stoi **czwarty cykl**. Jedyny nowy pomiar:
`HeuristicPolicy` z **ręcznymi** wagami **762,5 / 36,84**, +8,19% sparowanie, p10 **spadło**
(160,0 → 139,9), etykieta „bez zmian" (#57, `docs/cechy-planszy.md`). Rozgałęzienie w tacce:
śr. 39,66, mediana 32, max 170. Do celu ~13 000×. Sufit ruchów 2000, `capped_pct` 0,0
(#17, #51) — **nie podnosić**.

**Linia pracy: funkcja oceny planszy + przeszukanie bieżącej tacki + CEM offline na CPU.**
DQN zszedł z linii głównej w cyklu 3 (#40: 53 kroki/s, płaska krzywa, przeżycie gorsze od
zachłannej). Podstawa: `docs/research/kierunek-algorytmiczny.md` (#54). `agent.py` i `model.py`
**nie skasowane** — sieć może wrócić jako naprowadzanie przeszukiwania.

**PRÓG DECYZYJNY NA CYKL 5 — postawiony przed pomiarem, nie przesuwać.** Na wyniku #82
(`tray` + strojone wagi, 300 stałych seedów): **≥ 2100 śr.** → linia zostaje i iterujemy;
**1060–2100** → jedna iteracja poprawy przeszukania i ponowny pomiar, drugiego odroczenia nie ma;
**< 1060** → zmiana kierunku wg `docs/research/kierunek-algorytmiczny.md`, sekcja (a).
Patrz też: przeżycie z #82 osobno — 10 mln wymaga dłuższych partii, nie tylko lepiej punktowanych.

**Trzy pułapki mechaniki wykryte w cyklu 4. Nie powtórzyć ich.**

1. **`rola:bench` wykonuje polecenia z `## Weryfikacja` i nie ma innego wejścia**
   (`.github/loop/loop.py:414`). Wpisanie tam `ls bench/*.json` daje `done` bez policzenia
   czegokolwiek — tak zginęło #61. **W issue bencha `## Weryfikacja` to pełne wywołanie
   `python3 benchmark.py --candidate … --issue N`.** Zgłoszone właścicielowi jako **#83**.
2. **Raport sesji `crashed` kłamie o zawartości gałęzi.** #58 napisało „scommitowane: nic",
   a na `origin/task/58` leży `0877d99`. **Przy `crashed` zawsze `git log origin/task/<n>`.**
   Kod z niedokończonej sesji nie jedzie na `main` (`merge_main` scala kod tylko przy `done`);
   **odzyskanie go jest zadaniem orchestratora.**
3. **`## Weryfikacja` musi trafiać w ścieżkę wymuszoną przez skill roli.** `ls bridge/runs/*.json`
   zepchnęło wzorowo wykonane #60 do `partial`. Dla verifiera: `ls bridge/runs/*/*.json`.

**Niescalone gałęzie z dorobkiem — odzyskuje je cykl 4:** `origin/task/58` (`0877d99`:
`TrayPolicy`, `tray` w `benchmark.py`, testy), `origin/task/59` (`c7eaf92`:
`tools/tune_weights.py` — CEM, seedy treningowe rozłączne z benchmarkowymi, `TraySearchPolicy`),
`origin/task/60` (`d550db3`: `bridge/runs/d550db3/pomiar.json` — 33 pary plansza→tacka).

**`benchmark.py` nie umie wziąć strojonych wag.** `build_policy` (`benchmark.py:89-114`) zna
`random`/`greedy`/`heuristic`/`tray` z wagami **domyślnymi**, a ścieżkę do pliku ładuje jako
`state_dict` torcha. `weights.json` z CEM wyszedłby jako `ArmUnavailable`. Naprawia #80 i to
**musi być zrobione przed** jakimkolwiek issue bencha.

**Most żyje.** #60: 100/100 ruchów zgodnych z symulacją, zero zatrzymań na nieznanym oknie.
**Licznik `strata_okna` (#36): zero.** Ostatnie znane okna: reklama ✕ (285, 34), „Rating" ✕ (275, 192).
Verifier **nie może** uruchomić `bash tools/*.sh` (profil ma `adb`, `python`, `python3`, `git`);
odtworzenie kroków pojedynczymi komendami działa — wpisywać to w `## Budżet`, nie rozszerzać profilu.

**Decyzje zamknięte, nie otwierać ponownie:**

- **Kara terminalna:** `game.py:59` zwraca `gained` (#56, `670994e`, test w `tests/test_engine.py`).
  Suma nagród epizodu = `game.score`. Linia bazowa nietknięta (`benchmark.py:119-125` czyta `game.score`).
- **Kara za ruch nielegalny** (`game.py:52`): zostaje jako zapora. Kontrakt: polityka wybiera
  wyłącznie z `available_actions()` (zero wystąpień na 300 partii, #51).
- **Przeżycie: bez własnego składnika nagrody.** Zaszyte w cechach planszy (#57) i mierzone benchmarkiem.
- **Punktacja: `scoring.py` z gałęzi domyślnej** (zamrożone `0793735`, #20/#31). `eae980f`
  z `wayfinder/38-generator` **odrzucone** — 2,18% to inflacja miarki i zmieniłaby cel #9.
  Linia bazowa nie idzie do przeliczenia. Punkt #50 (testy po cherry-picku) **bezprzedmiotowy**.
- **57,3 kontra 34,99: rozstrzygnięte** (#52) — różne polityki, 300 vs 50 seedów. **34,99**
  jest przeżyciem w sensie #8; 57,3 nieporównywalne z niczym.
- **Obejście „wołaj `python` zamiast `python3`" jest nieaktualne** — właściciel dodał
  `Bash(python3:*)` (`5578467`, #67). Nie przepisywać go do nowych issues.

**Co wisi w powietrzu, po kolei ważności.**

1. **Czy linia dowozi krotności, czy procenty** — rozstrzyga #82 przy progu wyżej. Czwarty cykl
   bez odpowiedzi; przyczyną nie była linia, tylko trzy awarie mechaniki.
2. **Z-6: czy tacka zależy od stanu planszy.** Materiał zebrany (33 pary, #60), analiza w #78.
   `6f4b273` (`playability.py`) i `022d410` (tacka zależna od planszy) z `wayfinder/38-generator`
   **odroczone, nie odrzucone** — rozstrzyga pomiar, nie wybór gałęzi. Liczba 3104,74 z tamtej
   gałęzi miesza efekt punktacji z efektem generatora i **nic nie znaczy**.
3. **Z-5** (`docs/calibration-assumptions.md`) — nietknięte, wyżej niż wzór punktowy.
4. **Cache emulatora (#24) nie podpięty** — emulator wstaje na zimno.
5. **Ocena raportów badawczych: brak podstaw.** #54 nadal niesprawdzony pomiarem; cykl 3 nie
   zlecał `rola:researcher`, cykl 4 też nie. Jego teza jest twierdzeniem `[Z]` i idzie do
   pomiaru (#81, #82), nie do wdrożenia.

**Issues właściciela poza pętlą (bez `rola:*`) — nie brać ich jako zadań pętli:** #63, #64, #65
załatwione przez właściciela (`a1df79e`, `bc274fc`, `5578467`, `d89a5dd`, `7b4f856`). Otwarte
zgłoszenie z tego cyklu: **#83** (job bencha melduje `done` bez policzenia).

**Czego nie ruszam.** `.github/`, `bench/record.json`, `.claude/skills/orchestrator/`. Kodu nie
piszę. Sufit ruchów ×2 dopiero przy `capped_pct` > 5%, osobnym commitem, nigdy w dół. Weryfikację
na oryginale dopiero po ≥ 10 mln, nigdy dwa łańcuchy naraz. Sesje danych z mostu są od niej
niezależne i wolno je zlecać kiedykolwiek.

**`RAPORT.md`** nadpisany w cyklu 3 (`0bb2301`, 2026-09-25) — w cyklu 4 nie minęło 7 dni, nie ruszam.
