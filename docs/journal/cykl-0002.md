# Cykl 0002 — pętla domyka cykl i wykonuje w nim puste zadanie

Bilet: [#46](https://github.com/Kucze3205/ReinforcmentBlockBlast/issues/46) · gałąź `task/46` · 2026-09-24

Wejście na zimno było niepełne nie z mojej decyzji. `docs/journal/` na gałęzi domyślnej **nie
istnieje**: dziennik cyklu 1 leży niescalony na `task/42`. Przeczytałem go przez `git show`,
bo znałem nazwę z biletu — i to jest pierwszy fakt tego cyklu, nie ciekawostka. Mój własny
`.session/issue.md` też nie powstał; treść #46 wyciągnąłem przez `gh`, jak mój poprzednik.

Nie było otwartej `awarii` (#47 zamknięte). `report:unread` wisiało na czterech zamkniętych
issues — #42, #43, #44, #45 — i nie widziałem ich, dopóki nie zapytałem o stan `all`.
To jest pułapka do zapamiętania: `gh issue list --label report:unread` domyślnie pokazuje
tylko otwarte, a raport z definicji siedzi na zamkniętym.

## Zlecone

Cała mapa jest zablokowana awarią [#48](https://github.com/Kucze3205/ReinforcmentBlockBlast/issues/48).
Nie zadispatchowałem **niczego**, zgodnie z bramką: dopóki `awaria` jest otwarta, cisza.

| issue | rola | co |
|---|---|---|
| [#48](https://github.com/Kucze3205/ReinforcmentBlockBlast/issues/48) | `awaria` | `agent.sh` woła `gh` bez `GH_TOKEN`; żadna sesja nie dostaje zadania. Naprawa w `.github/`, poza zasięgiem każdego profilu. |
| [#49](https://github.com/Kucze3205/ReinforcmentBlockBlast/issues/49) | `rola:implementer` | Zielone testy na gałęzi domyślnej. `model.py:1` ciągnie `tkinter`. Zapora przed resztą mapy: bez tego nic się nie scala. |
| [#50](https://github.com/Kucze3205/ReinforcmentBlockBlast/issues/50) | `rola:implementer` | Rozjazd punktacji i generatora: gałąź domyślna kontra `wayfinder/38-generator`. Zaraportuj, nie naprawiaj. `effort:high`. |
| [#51](https://github.com/Kucze3205/ReinforcmentBlockBlast/issues/51) | `rola:implementer` | Ile sygnału gubią obie kary `-5` w `game.step`. Zmierz, nie naprawiaj. |
| [#52](https://github.com/Kucze3205/ReinforcmentBlockBlast/issues/52) | `rola:implementer` | 34,99 czy 57,3: która liczba przeżycia mierzy co innego, niż deklaruje. Zaraportuj, nie naprawiaj. |
| [#53](https://github.com/Kucze3205/ReinforcmentBlockBlast/issues/53) | `rola:researcher` | Kara terminalna i sygnał przeżycia: fakty spoza repo pod moją decyzję o nagrodzie. |
| [#54](https://github.com/Kucze3205/ReinforcmentBlockBlast/issues/54) | `rola:researcher` | Przeszukiwanie czy uczenie w 8×8 z tacką trzech klocków. `effort:high`. Materiał pod decyzję o kierunku. |
| [#55](https://github.com/Kucze3205/ReinforcmentBlockBlast/issues/55) | `rola:orchestrator` | Złączenie: domknięcie cyklu 2 i mapa cyklu 3. `model:opus`, `effort:high`. |

Krawędzie: **#49 ← #48**. **#50, #51, #52, #53, #54 ← #49**. **#55 ← #49, #50, #51, #52,
#53, #54** (sprawdzone odczytem `dependencies/blocked_by`: `[49,50,51,52,53,54]`).

Wszystko wisi na #49, i to nie jest nadmiarowa ostrożność. `merge_main` odrzuca scalenie
przy czerwonych testach, więc dopóki #49 nie przejdzie, **każde inne zadanie wróciłoby jako
`partial` z pracą uwięzioną na swojej gałęzi** — dokładnie to, co spotkało dziennik cyklu 1.
Front po zamknięciu #49 to pięć sesji przy suficie 12; #50, #51, #52 piszą do rozłącznych
plików, więc `conflict` im nie grozi.

Ról nie zatrudniałem. `rola:bench` nie zlecona: nikt w tym cyklu nie wyprodukował wag.
`rola:verifier` nie zlecona: weryfikację na oryginale zlecam dopiero po średniej ≥ 10 mln,
a jesteśmy cztery rzędy wielkości niżej.

## Liczby

| pozycja | wartość |
|---|---|
| rekord pętli | **brak** — `bench/record.json` nie istnieje, ani jednych wag |
| wynik kandydata w tym cyklu | brak — nikt nie produkował wag |
| cykli od ostatniego rekordu | nie dotyczy: rekordu nigdy nie było |
| linia bazowa (zachłanna, 300 stałych seedów) | **704,79** śr. / **34,99** postawień |
| sufit ruchów | 2000; `capped_pct` 0,0 — nie ma czego podnosić |
| cel | 10 000 000 śr. w symulatorze + jedna realna partia ≥ 1 mln |
| commitów sesji, które dojechały na gałąź domyślną | **0** przez dwa cykle |

Bez zmian wobec cyklu 1, bo w cyklu 2 nikt nie wykonał ani jednej linii pracy merytorycznej.
Ostatni wiersz jest jedyną nową liczbą i jest najważniejszą liczbą tego cyklu.

## Odkrycia i obalone założenia

**Naprawa zamka (#47) zadziałała i reszta mechaniki pętli jest potwierdzona.** To jest
domknięcie biegu na sucho, którego cykl 1 nie zdążył zobaczyć. Odcinek, który wtedy pozostał
niesprawdzony, przeszedł w całości: raport publikuje się komentarzem z markerem
`<!-- session-report -->`, etykieta `report:unread` się nakłada, issue się zamyka, zależne
odblokowują się same i epilog dispatchuje następne bez ręki człowieka. Dowód jest bezpośredni:
#45 i #46 nikt nie startował ręcznie, a ja tu jestem. **Jeden odcinek wciąż niesprawdzony:
czy commit sesji dojeżdża rebasem na gałąź domyślną** — bo dotąd żaden nie miał jak.

**Druga usterka z #47 została nietknięta i to ona wykonuje w pętli puste zadanie.**
`agent.sh` woła `loop.py export` w linii 13 i `loop.py publish` w podpowłoce z linii 16,
a `GH_TOKEN` eksportuje dopiero w linii 23. Wszystkie trzy sesje robocze cyklu 1 wróciły
ze statusem `blocked` i zerem zmian, każda tym samym zdaniem: katalog `.session/` jest pusty.
Moja też był. Naprawa musi podać token **tym dwóm wywołaniom**, a nie przesunąć `export`
przed linię 13 — globalny eksport wpuściłby token repo do procesu agenta w rolach, które
celowo go nie mają, a linia 23 istnieje właśnie po to, żeby dostawał go wyłącznie orchestrator.
Ta uwaga jest w #48, bo poprzednie zgłoszenie zostało naprawione połowicznie i nie chcę
powtórki.

**Trzecia rzecz, której nikt nie zgłosił, bo nikt jej jeszcze nie mógł zobaczyć: nic się
nie scala.** `loop.py merge_main` (linie 402–405) uruchamia po rebasie
`unittest discover -s tests` i przy niezerowym kodzie zwraca `"tests"`, co w `finalize`
(linia 493) zamienia status na `partial` **bez pushu na gałąź domyślną**. Testy są czerwone:
`model.py:1` ma `from turtle import done`, `turtle` ciągnie `tkinter`, którego na runnerze
nie ma, i import `agent` → `model` wywraca się w `test_engine`. Skutek jest cichy i
kumulatywny: dziennik cyklu 1 i pierwszy `RAPORT.md` leżą na `task/42`, ten dziennik
poleci na `task/46`, a każdy kolejny orchestrator startuje na zimno z pustym `docs/journal/`
i uznaje się za pierwszy cykl. **To jest amnezja pętli** i dlatego #49 blokuje całą resztę
mapy zamiast stać obok niej.

**Obalone założenie cyklu 1 o `scoring.py`.** Mój poprzednik napisał, że `scoring.py` nie
dostało poprawek, które #30 zmierzyło, i nazwał to najpoważniejszą rzeczą cyklu. Przeczytałem
`docs/calibration-assumptions.md` na gałęzi domyślnej i sprawa wygląda inaczej. Commit
`0793735` (#20, #31, ręką właściciela) **zamroził punktację symulatora** na pomiarze z
2026-09-21, wersja apki 10.7.5, gdzie Z-1, Z-2, Z-3 i Z-7 zgodziły się co do punktu w 18
ruchach z rzędu, i skasował wymóg R10. Obecny `scoring.py` jest z tym zamrożeniem zgodny:
`B(l) = 10` dla jednej linii, `10·l·(l−1)` dla większej liczby, combo jako **mnożnik** całego
bonusu. Wynik partii na oryginale liczy odtąd nasz wzór z trajektorii, a licznik apki jest
już tylko detektorem zmiany reguł. Wniosek: to nie jest zaniedbany dług, tylko **dwie
sprzeczne prawdy** — zamrożenie na gałęzi domyślnej kontra drabinka „10/15/20, combo rośnie
o liczbę linii" z commita `eae980f` na `wayfinder/38-generator`. Nie rozstrzygam tego
z dziennika ani z dwóch diffów; #50 ma je zestawić i pokazać skalę różnicy na konkretnym
ruchu.

**`wayfinder/38-generator` jest gałęzią-nadzbiorem i to zmienia wagę sprawy.** Niesie nie
tylko drabinkę (`eae980f`), ale też filtr grywalności tacki (`6f4b273`, `playability.py`)
i przepisany generator w modelu tacki zależnej od planszy (`022d410`), a do tego dwie
policzone linie bazowe w `bench/`. Tamtejsza zmiana `docs/calibration-assumptions.md` mówi
wprost, że po zamrożeniu punktacji **Z-5 i Z-6 stają się najważniejsze**, bo pula klocków
i rozkład losowania decydują, w jaką grę bot naprawdę gra, i żaden wybór wzoru punktowego
tego nie naprawi. To przesuwa ciężar z punktacji na generator — i jest powodem, dla którego
#50 ma `effort:high` i dotyczy obu rzeczy naraz, a nie samej drabinki. Uwaga dla następcy:
oba sporne commity ruszają `game.py`, czyli plik, w którym mieszka kształt nagrody; #50 ma
to sprawdzić z numerem linii, zanim ktokolwiek zacznie to przenosić.

**Raportów badawczych nadal nie było czego oceniać.** #43 miało być pierwszym; wróciło
`blocked` z pustym `.session/`, więc ocena użyteczności badań przechodzi na cykl 3 i jest
wpisana w kryteria #55. Dwa cykle bez ani jednego raportu merytorycznego to nie jest sygnał
o rolach — to sygnał o dostarczaniu zadań.

**`reward_shape_changed`: żaden raport nie zgłosił zmiany.** Dwa raporty implementera niosły
`reward_shape_changed: no` przy zerze zmian, więc pole nic w tym cyklu nie zmierzyło.
`git log -- game.py` kończy się na commicie kalibracyjnym `43d1e7c`: nagroda nie drgnęła.

## Decyzja

**Stawiam `awarię` (#48) po raz drugi w tej samej sprawie i nie dispatchuję niczego.**
Wyczerpałem to, co miałem do wyczerpania. Przeformułowanie zadania, zmiana roli, podział
ani zmiana linii pracy nie dotykają defektu, bo defekt nie dotyczy tego, **co** sesja ma
zrobić, tylko tego, że nie dowiaduje się o tym w ogóle. Zatrudnienie nowej roli odpada:
zakaz zapisu do `.github/` siedzi w workflow, więc taki profil zostałby odrzucony jako
`blocked`. Rozważyłem obejście, które byłoby w całości w moich rękach — wpisywać treść
zadania plikiem do repo i wskazywać go w `## Budżet` — i **odrzuciłem je**, bo taki plik
musiałby najpierw dojechać na gałąź domyślną, a nic tam nie dojeżdża. Obejście rozbiło się
o tę samą ścianę, co wszystko inne.

**Nie napisałem poprawki `model.py` sam, choć to jedna linia i choć to ona więzi mój własny
dziennik.** Kodu nie piszę i nie robię wyjątku dla wygodnego przypadku; wyjątek raz zrobiony
przestaje być wyjątkiem, a ja jestem jedyną rolą, której nikt nie recenzuje. Zamiast tego
zabezpieczyłem to, co ta decyzja kosztuje: **skompresowany `## Stan` wkładam do raportu
sesji przy #46**, a nie tylko do tego pliku. Raport jest komentarzem na issue, więc przeżyje
odrzucone scalenie; dziennik nie przeżyje. #55 ma w kryteriach napisane wprost, gdzie szukać,
jeżeli `docs/journal/` zastanie puste.

**Kolejność mapy jest wymuszona, nie wybrana.** #49 przed wszystkim, bo to jedyne zadanie,
którego scalenie **odblokowuje scalanie czegokolwiek innego** — i jednocześnie jedyny sposób
sprawdzenia ostatniego niepotwierdzonego odcinka mechaniki. Jeżeli #49 wróci `done`
i scalone, bieg na sucho jest domknięty w całości i pętla może zacząć pracować.

**Decyzji o obu karach `-5` i o sygnale przeżycia nie podejmuję drugi cykl z rzędu i mam
na to ten sam powód: materiał nie dojechał.** #51 dowozi pomiar, #53 dowozi fakty spoza repo.
Odraczam świadomie, z nazwaną brakującą rzeczą, nie przez przeoczenie.

**Decyzji o kierunku algorytmicznym też nie podejmuję — i to jest ostatni raz, kiedy takie
odroczenie jest uczciwe.** Wpisałem to wprost w kryteria #55: trzeci cykl bez tej decyzji
to już stagnacja, nie ostrożność. #54 jest po to, żeby następca miał z czego decydować,
i dlatego dostało `effort:high`.

**Rokowanie.** Linia pracy — „najpierw uczciwy sygnał nagrody i uczciwy symulator, potem
wybór metody" — **nadal rokuje merytorycznie, ale przestała rokować operacyjnie**, i to
rozróżnienie jest sednem tego cyklu. Merytorycznie: #40 pokazało, gdzie kończy się DQN na
runnerze bez GPU, symulator bez sieci robi ~10 tys. kroków/s, a to przesuwa ciężar dowodu
na przeszukiwanie — kierunek jest sensowny i #54 go rozstrzygnie. Operacyjnie: przez dwa
cykle pętla nie wykonała ani jednej linii pracy i nie scaliła ani jednego commita, a oba
powody są mechaniczne i oba leżą poza zasięgiem ról. **Zmiana linii pracy niczego by tu
nie naprawiła** — nie mam sygnału, że kierunek jest zły, mam sygnał, że maszyna nie jeździ.
Dlatego cykl 2 kończy się awarią i mapą gotową do puszczenia, a nie nową hipotezą. Jeżeli
po naprawie #48 i scaleniu #49 cykl 3 znów nie dowiezie liczby, **wtedy** rokowanie dotyczy
linii pracy, nie narzędzi.

## Stan

**Cel.** Średnia ≥ 10 mln na 300 stałych seedach przy ε = 0 (#8) **i** jedna realna partia
≥ 1 mln w apce (#9), liczona naszym wzorem z trajektorii mostu, nie licznikiem apki (#20, #31).
Oba potwierdzone raportami. Wtedy: plik `GOAL_REACHED`, przypięty issue, `RAPORT.md` końcowy,
koniec startowania sesji.

**Gdzie jesteśmy.** Rekordu nie ma — `bench/record.json` nie istnieje, ani jednych wag.
Najlepsze w repo to zachłanna heurystyka: **704,79 śr. / 34,99 postawień** na 300 stałych
seedach (`bench/43d1e7cf…json`, sha `43d1e7c`, #17); rotowane 690,36 / 35,31, `overfit_gap_pct`
2,05; losowa 59,15 / 11,9. Sufit ruchów 2000, `capped_pct` 0,0 — **nie podnosić**. Do celu
cztery rzędy wielkości. `game.py` nie zmieniony od `43d1e7c`: nagroda nie drgnęła.

**PĘTLA STOI — otwarta `awaria` [#48](https://github.com/Kucze3205/ReinforcmentBlockBlast/issues/48).**
`agent.sh` woła `loop.py` w liniach 13 i 16, a `GH_TOKEN` eksportuje w linii 23, więc
`.session/issue.md` nie powstaje dla żadnej roli i sesje wykonują puste zadanie. To druga
połowa #47, naprawiona połowicznie. Naprawa ma podać token tym dwóm wywołaniom, **nie**
przesuwać `export` (token wyciekłby do agenta w rolach bez `gh`). Dopóki #48 otwarte, nic
nie dispatchuję i dozorca też milczy.

**Amnezja pętli — druga zapora, tym razem w zasięgu ról.** `merge_main` (loop.py:402–405)
puszcza `unittest discover -s tests` po rebasie i przy czerwonych odrzuca scalenie
(`finalize`, linia 493 → `partial`). Testy są czerwone przez `model.py:1` `from turtle import done`
(ciągnie `tkinter`, brak na runnerze). **Zero commitów sesji dojechało na gałąź domyślną
przez dwa cykle**: dziennik cyklu 1 + pierwszy `RAPORT.md` są na `task/42`, dziennik cyklu 2
na `task/46`. Naprawia to #49, które blokuje całą resztę mapy.

**Mapa cyklu 2 czeka kompletna, nie odtwarzaj jej.** #49 ← #48. #50, #51, #52, #53, #54 ← #49.
#55 ← wszystkie. Po zamknięciu #48 dozorca (cron co 30 min, bramka `AUTOPILOT == on`) ruszy
#49 sam — ręczny dispatch niepotrzebny.

**Co wisi w powietrzu, po kolei ważności.**

1. **Dwie sprzeczne prawdy o punktacji i generatorze.** Gałąź domyślna: punktacja
   **zamrożona** (#20, #31, commit `0793735`), `B(l) = 10 / 10·l·(l−1)`, combo mnoży bonus,
   potwierdzone co do punktu na 18 ruchach z mostu (#18). Gałąź `wayfinder/38-generator`:
   drabinka „10/15/20, combo rośnie o liczbę linii" (`eae980f`), filtr grywalności tacki
   (`6f4b273`, `playability.py`), generator w modelu tacki zależnej od planszy (`022d410`),
   plus dwie policzone linie bazowe w `bench/`. **Cykl 1 mylnie nazwał to zaniedbanym
   długiem `scoring.py`** — to jest konflikt ustaleń, nie zaległość. Zestawia je #50.
   Oba sporne commity ruszają `game.py`, więc mogą ruszać nagrodę: #50 ma to sprawdzić
   z numerem linii. Po zamrożeniu punktacji **Z-5 i Z-6 (pula klocków, rozkład losowania)
   są ważniejsze od wzoru punktowego** — decydują, w jaką grę bot naprawdę gra.
2. **Obie kary `-5` w `game.step` nieratyfikowane.** Linia 52 = ruch nielegalny, linia 59 =
   normalny koniec partii, przy czym linia 59 wyrzuca `gained` policzone w linii 54. Materiał:
   #51 (pomiar) i #53 (fakty). **Decyzja należy do cyklu 3**, odroczona już dwa razy.
3. **Sygnał przeżycia** — czy własny składnik nagrody. Mapa #1 wiąże: „przeżycie ponad punkty".
   Materiał z #53 (c). Decyzja razem z pozycją 2.
4. **Kierunek algorytmiczny nierozstrzygnięty, drugi cykl.** #40: DQN na runnerze bez GPU
   nierealny jako metoda główna — 53 kroki/s (70% czasu to SGD po jednej próbce), krzywa
   płaska po 80 tys. kroków, przeżycie gorsze od zachłannej, checkpoint z replayem 187 MB.
   Symulator bez sieci ~10 tys. kroków/s. Znane usterki stamtąd: `agent.train()` się wywraca,
   seed 42 na sztywno. Materiał: #54. **Trzeci cykl bez tej decyzji to stagnacja.**
5. **Dwie liczby przeżycia zachłannej: 34,99 (benchmark) vs 57,3 (#40).** Tanie, a zatruwa
   każde porównanie międzymetodowe. Rozstrzyga #52.
6. **Cache emulatora (#24) nie podpięty** — verifier startuje zimno. Do decyzji, gdy verifier
   wróci do gry; nie był zlecany od dwóch cykli.

**Pułapki, których nie powtarzaj.**

- `gh issue list --label report:unread` domyślnie pokazuje **tylko otwarte**, a raport siedzi
  na zamkniętym. Pytaj o `--state all`, inaczej zobaczysz zero i uznasz, że nic nie wróciło.
- `docs/journal/` na gałęzi domyślnej może być puste mimo istnienia dzienników. Zanim uznasz
  się za pierwszy cykl, sprawdź `git log --all -- 'docs/journal/*'` i raport sesji przy #46.
- `gh api .../actions/variables/AUTOPILOT` zwraca 403 pod `GITHUB_TOKEN` sesji — nie da się
  stąd odczytać, czy autopilot jest włączony. Nie wyciągaj z tego wniosków.

**Czego nie ruszam.** `.github/`, `bench/record.json`, `.claude/skills/orchestrator/`.
Kodu nie piszę — także wtedy, gdy poprawka ma jedną linię i więzi mój własny dziennik.
Sufit ruchów podnoszę ×2 dopiero przy `capped_pct` > 5% — dziś 0. Weryfikację na oryginale
zlecam dopiero po średniej ≥ 10 mln, nigdy dwa łańcuchy naraz.

**Licznik strat na okno (#36).** Zero. Żadnego zatrzymania mostu; `rola:verifier` nie była
zlecana od dwóch cykli. Ostatnie znane okna: reklama ✕ (285, 34), „Rating" ✕ (275, 192) —
stałe zmierzone, nie regułą.

**Stan pętli.** `awaria`: **#48, otwarta**. `report:unread`: brak (zdjęte z #42, #43, #44, #45).
Otwarte issues: #48 (awaria), #49–#54 (mapa cyklu 2, zablokowane), #55 (złączenie).
Poza polem widzenia, nietknięte: #1 (mapa), #19 (bilet biegu na sucho właściciela).
